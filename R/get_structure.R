##' @title Get structure

##' @return
##' @author Shir Dekel
##' @export
get_structure <- function() {
  list(
      list(
          c("decentralised", "centralised"),
          c("centralised", "decentralised")
      ),
      list(
          c("decentralised", "centralised"),
          c("decentralised", "centralised")
      )
  )
}
